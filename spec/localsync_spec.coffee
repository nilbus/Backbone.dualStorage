window = require('./spec_helper').window

describe 'localsync', ->
  describe 'standard Backbone.sync methods', ->
    xit 'creates records', ->
    xit 'reads records', ->
    xit 'updates records', ->
    xit 'deletes records', ->

  describe 'extra methods', ->
    xit 'clears out all records from the store', ->
    xit 'reports whether or not it hasDirtyOrDestroyed', ->

  describe 'options', ->
    xit 'ignores callbacks with the ignoreCallbacks option set', ->
    xit 'uses the dirty option to mark the dirty/clean state on update and destroy', ->
