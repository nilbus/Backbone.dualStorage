window = require('./spec_helper').window

describe 'window.Store', ->
  describe 'creation', ->
    xit 'takes a name in its constructor', ->

  describe 'offline', ->
    xit 'marks records dirty and clean', ->
    xit 'marks records destroyed', ->
    xit 'reports if it hasDirtyOrDestroyed records', ->

  describe 'persistence', ->
    xit 'fetches records by id with find', ->
    xit 'fetches all records with findAll', ->
    xit 'clears out its records', ->
    xit 'creates records', ->
    xit 'updates records', ->
    xit 'destroys records', ->
